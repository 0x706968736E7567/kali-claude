# Copy to .env and fill in. .env is gitignored.
#
# Optional: leave unset to log in interactively with `claude` instead (auth
# persists in the claude-config Docker volume across restarts). If set, the key
# is exposed to every process in the container — rotate it after risky work.
ANTHROPIC_API_KEY=sk-ant-...
